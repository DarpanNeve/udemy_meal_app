import 'package:flutter/material.dart';

class MealScreen extends StatelessWidget {
  const MealScreen({super.key,required});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
